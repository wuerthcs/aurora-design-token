{
  "name": "aurora-tokens",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon ./index.js -e js,yaml,json --ignore dist/ --ignore node_modules",
    "build": "node ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "./dist"
  ],
  "keywords": [],
  "author": "Dominik Biedebach",
  "license": "MIT",
  "dependencies": {
    "theo": "^8.1.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
