{
  "name": "@zip-software/aurora-tokens",
  "version": "1.0.11",
  "description": "",
  "main": "dist/web.js",
  "types": "./tokens.d.ts",
  "scripts": {
    "start": "nodemon ./build.js -e js,yaml,json --ignore dist/ --ignore node_modules --ignore tokens.d.ts --ignore tokens.js",
    "build": "node ./build.js && npm run typings",
    "prepublish": "npm run build",
    "typings": "tsc --declaration --allowJs --outFile ./tokens.js ./dist/web.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "README.md",
    "public",
    "dist",
    "example.html",
    "tokens.js",
    "tokens.d.ts"
  ],
  "keywords": [],
  "author": "ZIP-Software",
  "license": "MIT",
  "dependencies": {
    "theo": "^8.1.5",
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
